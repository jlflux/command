# Uploaded tenant files — never in git (keep the .htaccess guard, though)
public/uploads/*
!public/uploads/.htaccess

# Local overrides / editor noise
*.local.php
.DS_Store

# Portable local dev environment (created by dev/windows/setup.bat)
local-dev/
